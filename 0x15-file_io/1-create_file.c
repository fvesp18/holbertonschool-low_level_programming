#include "holberton.h"
/**
 * create_file - creates file
 * @filename: file of name to be created
 * @text_content: data inside said file
 * Return: -1, 1
 */
int create_file(const char *filename, char *text_content)
{
	if (filename == NULL)
		return (-1);
	if (!text_content)
		return (-1);

	return (0);
}
