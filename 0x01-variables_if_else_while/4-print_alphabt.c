#include<stdio.h>

/**
 * main - if q or e , dont print if a - z putchar
 *
 * Return: 0
 */

int main()
{
	char ch;

	for(ch = 97; ch <=122; ch++)
		putchar(ch);
	if (ch = 113 || ch = 101)
		putchar!(ch);
	return (0);
}
