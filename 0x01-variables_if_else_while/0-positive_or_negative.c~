/**
 * main - else if, if, else prints if > = or < 0
 *
 * Return: 0;
 */

#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(void)
{
	if (n > 0)
		printf("%d is positive\n", n);
	else if (n == 0)
		printf("%d is zero\n", n);
	else
		printf("%d is negative\n", n);

	return (0);
}
