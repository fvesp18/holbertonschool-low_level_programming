#include <stdlib.h>
#include <stdio.h>

int main(int argc, char **argv)
{
	int ind;
	int prod = 0;

	if (argc > 1)
	{
		for (ind = 1; ind < argc; ind++)
		{
			prod *= atoi(argv[ind]);
		}
		printf("%d\n", prod);
	}

	if (argc == 1)
		printf("Error\n");

       	return (1);

}
