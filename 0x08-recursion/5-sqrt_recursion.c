#include "holberton.h"

/**
 * _sqrt_recursion - square root int n
 * @n: base int
 * Return: -1
 */

int _sqrt_recursion(int n)
{

	int sind = 1;
	int eind = x;
	int temp = 0;

	_sqrt_recursion(n/2);
	if (_sqrt_recursion != temp)
		_sqrt_recursion ((n/temp + temp) / 2)

_sqrtr
}
